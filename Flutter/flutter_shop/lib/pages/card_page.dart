import 'package:flutter/material.dart';
class CatrdPage extends StatelessWidget {
  const CatrdPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("购物车页面"),
      ),
    );
  }
}